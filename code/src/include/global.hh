#ifndef __GLOBAL__
#define __GLOBAL__


#include <iostream>
#include <string>
#include <fstream>
#include <random>
#include <ctime>
#include <chrono>		// Using high-resolution timer


//Variables globales del proyecto
struct Global {
	static bool debug;
};

#endif